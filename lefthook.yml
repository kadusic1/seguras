pre-commit:
  parallel: true
  commands:
    format:
      run: bunx biome check --write --no-errors-on-unmatched --staged && git add -u
      glob: "*.{js,ts,jsx,tsx,json,md}"
    type-check:
      run: bun run type-check
