{
	# email you@yourdomain.com
	# ^ uncomment and fill in once a real domain exists
}

localhost {
	encode zstd gzip

	header {
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
	}

	reverse_proxy frontend:3000
}